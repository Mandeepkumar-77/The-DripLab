<<<<<<< HEAD
name: Auto-Deploy to gh-pages
=======
name: Deploy DripLab to GitHub Pages
>>>>>>> 5c999a6 (chore: Automate CI/CD Pipeline via GitHub Actions)

on:
  push:
    branches:
      - main
    tags:
      - 'v*'
<<<<<<< HEAD

jobs:
  deploy:
    runs-on: ubuntu-latest
    permissions:
      contents: write
=======
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: 'pages'
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
>>>>>>> 5c999a6 (chore: Automate CI/CD Pipeline via GitHub Actions)
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

<<<<<<< HEAD
      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install Dependencies and Build
        run: |
          npm ci
          npm run build

      - name: Deploy to gh-pages branch
        uses: peaceiris/actions-gh-pages@v4
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./dist
          force_orphan: true
=======
      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install Dependencies
        run: npm ci

      - name: Build Vite App
        run: npm run build

      - name: Setup GitHub Pages
        uses: actions/configure-pages@v5

      - name: Upload Artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: './dist'

  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    needs: build
    runs-on: ubuntu-latest
    name: Deploy
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
>>>>>>> 5c999a6 (chore: Automate CI/CD Pipeline via GitHub Actions)
